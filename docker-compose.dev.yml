services:
  orion:
    build:
      context: .
      target: dev
    ports:
      - "3005:3005"
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/frontend/node_modules
      - orion-data:/app/data
