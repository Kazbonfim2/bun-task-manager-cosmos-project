services:
  orion:
    build:
      context: .
      target: dev
    ports:
      - "${PORT:-3005}:${PORT:-3005}"
    environment:
      NODE_ENV: development
    volumes:
      - .:/app
      - /app/backend/node_modules
      - /app/frontend/node_modules
      - orion-data:/app/data

