services:
  orion:
    build:
      context: .
      target: dev
    ports:
      - "3005:3005"
    environment:
      NODE_ENV: development
    volumes:
      - .:/app
      - /app/backend/node_modules
      - /app/frontend/node_modules
      - orion-data:/app/data
